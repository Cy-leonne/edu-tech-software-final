import AdminDashboard from './AdminDashboard';

/** Dedicated SuperAdmin route using the original navigation shell and icons. */
const SuperAdminPortal = () => <AdminDashboard basePath="/SuperAdmin" />;

export default SuperAdminPortal;
